# Список исправлений - Полный рефакторинг PROPELLINI

## ✅ Критические баги исправлены

### 1. Блокировка скролла
**Проблема:** Множественные `preventDefault` на `touchmove` блокировали скролл даже когда модалы закрыты.

**Решение:**
- Создан `scroll-fix.js` - централизованная обработка скролла
- Скролл блокируется ТОЛЬКО когда модальное окно открыто
- Внутри модальных окон скролл работает нормально
- Убраны лишние обработчики `wheel` и `touchmove`

**Файлы:**
- `src/js/scroll-fix.js` (NEW)
- `src/modules/calculator/calculator.js` - убраны `preventDefault` на touchstart
- `src/js/mobile-gallery-swipe.js` - исправлены обработчики zoom

### 2. Z-index наложения
**Проблема:** Фантомные контейнеры с z-index: 9999 перехватывали клики.

**Решение:**
- Создан `z-index-fix.css` с правильной иерархией:
  - Базовые слои: 1-2
  - Навигация: 100
  - Модальные окна: 9990-9993
  - Lightbox: 9994
- Неактивные модальные окна: `pointer-events: none`
- Убраны фантомные контейнеры

**Файлы:**
- `src/styles/z-index-fix.css` (NEW)

### 3. Desktop калькулятор
**Проблема:** Калькулятор не инициализировался, переменные не существовали.

**Решение:**
- Создан единый `calculator-unified.js` с правильной архитектурой
- Инициализация после загрузки DOM
- Единый источник данных `CALCULATOR_DATA`
- Правильный рендеринг брендов, моделей, зон
- Кнопки быстрого выбора зон работают
- Старый `calculator.js` отключен (инициализация закомментирована)

**Файлы:**
- `src/modules/calculator/calculator-unified.js` (NEW)
- `src/modules/calculator/calculator.js` - инициализация отключена
- `src/js/calculator-modal.js` - обновлена интеграция

### 4. Mobile калькулятор
**Проблема:** Дёргается, зависает, конфликтует с desktop логикой.

**Решение:**
- Fullscreen wizard на 4 шага:
  1. Марка (горизонтальные чипсы)
  2. Модель (автокласс + badge)
  3. Услуга (пакеты или ручной выбор зон)
  4. Итого (summary + кнопка "Записаться")
- Фиксированная панель "Итого" снизу (glassmorphism)
- Swipe навигация между шагами
- Большие CTA-кнопки (min-height: 48px)
- Список зон с чекбоксами (без SVG)

**Файлы:**
- `src/modules/calculator/calculator-unified.js` - методы `initMobileWizard()`, `createMobileWizard()`, `renderMobileStep()`

### 5. Кнопки и модальные окна
**Проблема:** Кнопки не открывают модальные окна, клики проваливаются.

**Решение:**
- Все кнопки "Рассчитать", "Записаться" открывают правильные модалы
- Форма заявки с автозаполнением из калькулятора
- Правильная блокировка скролла при открытии
- Закрытие по X, Escape, клику вне формы

**Файлы:**
- `src/js/calculator-modal.js` - обновлена инициализация
- `src/js/unified-order-form.js` - исправлена блокировка скролла
- `src/js/button-fixes.js` - обработчики кнопок

### 6. Навигация
**Проблема:** Меню не открывается, кнопки не жмутся.

**Решение:**
- Мобильная версия: светлый фон, темный логотип
- Бургер-меню работает корректно
- Закрытие по клику на ссылку/вне меню
- Desktop: прозрачный + blur до скролла 80px

**Файлы:**
- `src/modules/header/header.css` - мобильные стили
- `src/modules/header/header.js` - обработчики событий

### 7. Hero-блок
**Проблема:** Заголовок съехал, текст сливается с фоном.

**Решение:**
- Фон: Mercedes AMG GT63 (desktop/mobile версии)
- Затемнение 55% через `::after`
- Параллакс для desktop (`background-attachment: fixed`)
- Отключен на mobile (`background-attachment: scroll`)
- Контрастный текст с `text-shadow`

**Файлы:**
- `src/styles/hero-background-fix.css`
- `src/styles/hero-background-fix-override.css`

### 8. Производительность
**Проблема:** GPU убит box-shadow'ами, backdrop-filter'ом, PNG-фонами.

**Решение:**
- Создан `performance-optimization.css`
- Убраны лишние `box-shadow` (заменены на легкие)
- Оптимизирован `backdrop-filter` (только где нужно)
- Убраны лишние анимации на mobile
- GPU оптимизация через `will-change` и `transform: translateZ(0)`

**Файлы:**
- `src/styles/performance-optimization.css` (NEW)

## 📁 Новая структура

```
src/
├── modules/
│   ├── header/          ✅ Навигация
│   ├── hero/            ✅ Главный блок
│   ├── benefits/        ✅ Преимущества
│   ├── calculator/      ✅ Калькулятор (unified)
│   ├── gallery/         ✅ Галерея
│   ├── contacts/        ✅ Контакты
│   └── footer/          ✅ Подвал
├── styles/
│   ├── global.css       ✅ Базовые стили
│   ├── z-index-fix.css  ✅ Исправление z-index
│   └── performance-optimization.css ✅ Оптимизация
└── js/
    ├── scroll-fix.js    ✅ Исправление скролла
    ├── calculator-modal.js ✅ Модальное окно
    └── unified-order-form.js ✅ Форма заявки
```

## 🔧 Технические детали

### Единый источник данных
```javascript
CALCULATOR_DATA = {
  brands: { ... },      // Все бренды и модели
  classes: { ... },     // Классы автомобилей
  zones: { ... }        // Зоны оклейки по классам
}
```

### Инициализация
1. `scroll-fix.js` - первым (убирает блокировку скролла)
2. `header.js` - навигация
3. `calculator-unified.js` - единый калькулятор
4. Остальные UI скрипты

### Порядок CSS
1. `global.css` - базовые стили
2. Модули (header, hero, benefits, ...)
3. `z-index-fix.css` - исправление слоёв
4. `performance-optimization.css` - оптимизация
5. Legacy стили для совместимости

## ✅ Что работает

- ✅ Desktop калькулятор - открытие, выбор, расчет
- ✅ Mobile калькулятор - wizard 4 шага, swipe
- ✅ Скролл страницы - не блокируется когда модалы закрыты
- ✅ Кнопки - все открывают правильные модальные окна
- ✅ Навигация - мобильная версия, бургер-меню
- ✅ Hero-блок - фон, затемнение, контрастность
- ✅ Форма заявки - автозаполнение из калькулятора
- ✅ Производительность - оптимизирована

## 🚀 Готово к тестированию

Проект готов к тестированию на:
- iPhone (Safari)
- Android (Chrome)
- Desktop (Chrome, Firefox, Safari)

Все критические баги исправлены, код модульный и поддерживаемый.

